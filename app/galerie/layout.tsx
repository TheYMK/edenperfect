import type { Metadata } from "next";
export const metadata: Metadata = { title: "Galerie", description: "Nos réalisations en images : coiffure, soins visage, nail art, extensions de cils. Galerie photo Eden Perfect Paris." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
